import React from 'react';
import moment from 'moment'
import {  Box } from '@chakra-ui/react';

function VideoLength({ time }) {
    // const length = time > 3599 ? moment().startOf("day").seconds(time).format("HH:mm:ss") : moment().startOf("day").seconds(time).format("mm:ss");
    return (
      //   <Box h='30px' w={'max-content'}  bg='black' borderRadius={'10px'} p='3px 10px 2px 10px' >
      // {length}
      // </Box>
      <div></div>

    )
}

export default VideoLength;